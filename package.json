{
  "name": "airtable-webhook-relay",
  "version": "1.0.0",
  "description": "Relay server to connect Claude or custom tools to Airtable via Make.com",
  "main": "webhook-relay-server.js",
  "scripts": {
    "start": "node webhook-relay-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.6.9"
  }
}
