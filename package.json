{
  "name": "airtable-webhook-relay",
  "version": "1.0.0",
  "description": "Webhook relay server for Airtable via Make.com",
  "main": "webhook-relay-server.js",
  "scripts": {
    "start": "node webhook-relay-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
